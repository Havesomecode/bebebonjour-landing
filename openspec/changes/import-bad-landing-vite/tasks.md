## 1. Project Setup

- [x] 1.1 Initialize a root-level Vite application structure and declare dependencies.
- [x] 1.2 Add or update Vercel deployment configuration for Vite build output.
- [x] 1.3 Initialize a Git repository at the workspace root if one does not exist.

## 2. Landing Migration

- [x] 2.1 Review `bad-implem` commit `2cccc8b` and identify reusable page/assets.
- [x] 2.2 Migrate reusable landing assets and implementation into the root Vite app.
- [x] 2.3 Remove the legacy `bad-implem/` folder after successful migration.

## 3. SPEC Alignment

- [x] 3.1 Update page structure to include all required sections from `SPEC.md`.
- [x] 3.2 Replace landing copy to match the required French text, offers, FAQ, and final CTA.
- [x] 3.3 Verify all CTA labels and trust proof line match `SPEC.md`.

## 4. Validation

- [x] 4.1 Run a production build and confirm Vite output is generated for Vercel deployment.
- [x] 4.2 Perform a final content and structure check against the capability spec.
